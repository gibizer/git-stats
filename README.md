```
usage: stat.py [-h] [--repo REPO] [--filter FILTER] [--branch BRANCH]

Calculate git stats

options:
  -h, --help       show this help message and exit
  --repo REPO      Path of the git repository
  --filter FILTER  Filter the commits by a substring in the commit message
  --branch BRANCH  The branch to search on. Defaulted to master
```
