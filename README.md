```
$ python stat.py  -h
usage: stat.py [-h] [--repo REPO] [--filter FILTER]

Calculate git stats
  
optional arguments:
  -h, --help       show this help message and exit
  --repo REPO      Path of the git repository
  --filter FILTER  Filter the commits by a substring in the commit message
```
